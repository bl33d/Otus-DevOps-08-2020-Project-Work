[gitlab_hosts]
%{ for ip in instance_ip_gitlab ~}
gitlab-host ansible_host=${ip}
%{ endfor ~}

[monitoring_hosts]
%{ for ip in instance_ip_monitoring ~}
monitoring-host ansible_host=${ip}
%{ endfor ~}

[logging_hosts]
%{ for ip in instance_ip_logging ~}
logging-host ansible_host=${ip}
%{ endfor ~}
